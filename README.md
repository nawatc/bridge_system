# Bridge_system

# Install requirement
  for Windows
  
  cd docs && pip_windows.bat
  
  for Debian
  
  cd docs && pip_debian.sh
  
# Run program
  python -u bridge-system-gui.py

# Build program
  for Windows
  
  cd docs && make.bat
